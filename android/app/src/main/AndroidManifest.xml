<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Izin untuk akses internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- Izin untuk meminta instalasi paket dari sumber tidak dikenal -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    
    <!-- Izin untuk menjadwalkan notifikasi presisi -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    
    <!-- Izin untuk menampilkan notifikasi di Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    
    <!-- Izin untuk menjalankan service setelah boot untuk menjadwalkan ulang notifikasi -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Receiver untuk notifikasi terjadwal -->
        <receiver
              android:exported="false"
              android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />

        <!-- Receiver untuk boot ulang perangkat -->
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!-- Receiver untuk menangani aksi notifikasi (misal: tombol) -->
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver" />

        <service
            android:name="com.dexterous.flutterlocalnotifications.ForegroundService"
            android:exported="false"
            android:stopWithTask="false"/>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
            
        <!-- ID Aplikasi AdMob -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-9773465799516929~5575068959"/>

        <!-- DITAMBAHKAN: ID Game Unity untuk Mediasi AdMob -->
        <meta-data
            android:name="com.google.android.gms.ads.unity.GAME_ID"
            android:value="80000716" />
    </application>

    <!-- Konfigurasi Visibilitas Paket (Krusial untuk Android 11+) -->
    <queries>
        <!-- Diperlukan untuk sistem interaksi teks bawaan Flutter -->
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        
        <!-- WAJIB TAMBAH: Agar open_filex diizinkan memicu instalasi APK -->
        <intent>
            <action android:name="android.intent.action.INSTALL_PACKAGE" />
            <data android:scheme="file" android:mimeType="application/vnd.android.package-archive" />
        </intent>
    </queries>
</manifest>